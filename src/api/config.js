// import Config from 'react-native-config';

const config = {
  // API_URL: `${Config.SERVER_BASE_URL}/api/v1`,
  // SOCKET_URL: `${Config.SERVER_BASE_URL}`,
  // API_URL_V2: `${Config.SERVER_BASE_URL}/api/v2`,
};

export default config;
